# Проверка MVP

Проверено 23 сентября 2026 в локальном клоне `/Users/admin/hack-001016ea-intent`.

## Повторная приёмка перед финальным коммитом — 23.09.2026

- Текущие исходники: `bash scripts/check.sh` — **116 passed, 0 skipped, 22,94 с**; `pip check`, TypeScript и Vite production build успешны. Пять предупреждений SWIG/PyMuPDF не являются падениями тестов.
- Проверен отдельный снимок всех 66 отслеживаемых и новых неигнорируемых файлов, без `.env`, рабочей базы и оригиналов организатора: **93 passed, 23 skipped, 4,45 с**, production build успешен. В этом повторном прогоне использованы уже установленные зависимости из lock-файлов; это не новая установка зависимостей. Предыдущая свежая установка описана ниже.
- Снимок запущен через `scripts/start.sh` на отдельном порту и с новой SQLite. Health подтверждает отключённый внешний AI и отсутствие локальной реальной пары. В браузере проверены открытие контрольных 3 + 3 DOCX, локальное расследование, точные §7.2.2 и §2.3, блокировка пустого решения и сохранение `NEEDS_INFO` с автором и обоснованием. Заключение показывает решение; ошибок и предупреждений JavaScript нет.
- Через HTTP в этот же экземпляр загружены оригинальные DOCX: 325 функций, 203 записи ответственности, 1 421 организационная проверка; 121 сохранение, 2 переноса, 1 потенциальный пробел, 1 потенциальный конфликт и 57 выводов для ручной проверки. Локальное расследование пробела выполнило 16 шагов. Повторная загрузка в обратном порядке возвращает тот же record из кэша.
- JSON, CSV и Markdown выгружаются с HTTP 200. После решения в браузере SHA-256 канонического JSON не изменился; решение присутствует в отдельном журнале и Markdown. Рабочая база пользователя в проверке не использовалась.
- Проверены состав будущего коммита, относительные ссылки README, исключение локальных данных и ключей; `git diff --check` без ошибок. Исправлена устаревшая подпись версии в боковой панели: v2.1 соответствует движку 2.1.0.

Технический вывод: прототип готов к сдаче в заявленных границах. Проверены обязательные сценарии предоставленного задания трека 11 и требования самостоятельного запуска из README. Открытая страница положения проверена; подробный текст задания на сайте требует регистрации, поэтому сверка функциональных требований выполнена по предоставленному организатором `track_11.txt`. Выполнение регистрационных требований, указаний в закрытых каналах и независимая предметная приёмка этим прогоном не подтверждаются. Live-вызов внешней модели не проверялся; отсутствие смыслового совпадения само по себе не доказывает потерю функции.

## Финальная проверка сдачи — 23.09.2026

Проверено на macOS ARM64, Python 3.9.6, Node.js 22.9.0, pnpm 9.15.0. Backend `orgx-2.1.0`, политика `evidence-policy-2.1.0`.

- **Полный набор с оригиналами: 116 passed, 0 skipped, 23,70 с.** Пять предупреждений относятся к SWIG/PyMuPDF; падений нет. База проверки изолирована от рабочей базы аудитора, внешний AI отключён.
- **Свежая установка:** создан снимок всех отслеживаемых и новых неигнорируемых файлов проекта без `.git`, `.venv`, `node_modules`, `.env`, `work` и оригинальных DOCX. В новом virtualenv зависимости установлены из `requirements.lock`; frontend установлен через `pnpm install --frozen-lockfile`. Выполнен `scripts/setup.sh`, `pip check` — без конфликтов. Для pnpm использовано отдельное хранилище в папке проверки.
- **Чистый снимок: 93 passed, 23 skipped, 4,45 с**, затем TypeScript и Vite production build успешны. Все 23 пропуска связаны с отсутствующими оригиналами организатора; это ожидаемый режим чистого клона и не заменяет полный source-прогон выше.
- После последней правки текста журнала кандидатов и русских подписей выполнены **31 targeted-тест слоя ответственности — passed**. Эта правка добавляет имя владельца/текст кандидата в журнал, не меняет классификацию. Production-сборка повторена скриптом запуска, успешна.
- **Запуск именно по README:** `PORT=8016 ORGX_ENABLE_OPENAI=0 bash scripts/start.sh` в чистом окружении; frontend пересобран, Uvicorn запущен на localhost. Личных аккаунтов, ключа API и прежней SQLite нет.
- **Браузер, синтетический комплект:** шесть DOCX, 9 записей ответственности, 63 организационные проверки (2 записи PASS, 7 REVIEW); реальное выполнение локального расследования, чтение кандидата и контрдоказательств, переход к точным §7.2.2 и §2.3, блокировка пустого решения, сохранение `NEEDS_INFO` с автором и обоснованием, заключение с прогрессом 1/6. HTTP-выгрузки JSON/CSV/Markdown возвращают 200; Markdown включает решение, канонический JSON сохраняет `PENDING` у системных выводов.
- **Браузер, реальные DOCX:** оба оригинала выбраны через file chooser и загружены в чистый экземпляр. Получены 981 фрагмент, 325 извлечённых функций, 182 вывода. История ответственности: 203 записи и 1 421 проверка; 118 записей PASS, 85 REVIEW, 0 FAIL. Выполнено расследование пробела явного права ДККМ: поиск по 490 фрагментам новой версии, проверка кандидатов и контрдоказательств. Источники §5.6.2/§5.6 «до» и §5.5.2/§10.7 «после» открываются с точным текстом и именем файла. Подразделения: ДНМ/ДККМ сохранены, ДИТААД/ДОА добавлены; изменение ролей показано отдельно.
- В проверенных браузерных сценариях **0 ошибок и предупреждений JavaScript**. Сервер проверки и созданная вкладка корректно остановлены; тестовые решения не внесены в пользовательскую рабочую базу.
- **Benchmark реальной пары:** cold 2,187 с, warm 0,202 с; канонические результаты идентичны. Это измерение этой машины, не SLA. 121 сохранение, 2 переноса, 1 потенциальный пробел, 1 потенциальный конфликт, 57 выводов для ручной проверки.

Live-вызовы OpenAI не выполнялись. Function calling проверен fake Responses-клиентом; качество конкретной внешней модели не измерено. `R-ID` — текстовый fingerprint, а `SPLIT`/`MERGED` — гипотезы буквального покрытия. Истинность организационных выводов и независимый human signoff этими инженерными проверками не подтверждаются.

## Предыдущая интеграционная проверка (до legacy-addon)

- Полный прогон после объединения: **115 passed**, 5 предупреждений SWIG/PyMuPDF, 23,11 с. Версии `orgx-2.1.0` / `evidence-policy-2.1.0`. Это инженерный прогон на текущей машине.
- Контрольный комплект `examples/control`: 3 файла до и 3 после, 1 сохранение, 1 перенос, 1 возможный пробел, 1 новый дубль без предшественника, 1 конфликт разрешения/запрета, 1 частичное пересечение для ручной проверки; 1 сохранённое, 1 добавленное и 1 явно преобразованное подразделение. Источники синтетические, не описывают Казахтелеком.
- Проверены порядок файлов и одинаковый canonical JSON, полный manifest поиска, охват всех извлечённых функций в матрице, дубли загрузок, предел количества файлов, экспорты CSV/Markdown и неизменность аудита после решения.
- Добавлены 6 проверок коротких разрешений/запретов: совпадающий scope даёт риск конфликта даже для двух слов; различие филиала блокирует такой вывод.
- 30 проверок слоя ответственности: fingerprints при перенумерации, UNKNOWN при неоднозначности, split/merge только как гипотезы, семь проверок, повреждение цитат/хэшей/ссылок, API без изменения canonical record.
- AI tool loop проверен mock-клиентом: инструменты, обязательные поиск контрдоказательств и проверка владельцев, ID и schema validation, лимит действий, replay кэша, fallback без утечки текста ошибки. **Реальные вызовы OpenAI не выполнялись**, качество ответов конкретной модели не заявляется проверенным.
- UI QA на отдельной локальной SQLite: синтетический аудит из шести файлов, матрица из 9 строк, точные цитаты правильных документов, журнал поиска и отключённый AI, блокировка пустого решения, сохранение `NEEDS_INFO`, история и прогресс 1/6 в заключении. Ошибок JavaScript в просмотренном сценарии нет. Сервер и созданная вкладка закрыты перед следующими тестами.
- Набор source-truth: прежние 22 ожидания не изменялись; ДНМ/ДККМ сохранены, ДИТААД/ДОА добавлены. `human_signoff=null`.

Проверки подтверждают перечисленные сценарии и свойства. Они не являются независимой экспертной приёмкой, оценкой recall на неизвестном корпусе или доказательством универсальности парсера. Поддержка произвольных оргсхем/сканов, семантическая эквивалентность всех перефразирований и истинность сведений загруженного документа не заявляются.

## Предыдущий этап после перезапуска: устойчивость (история)

- Движок `orgx-1.1.0`, политика `evidence-policy-1.1.0`.
- Новый полный прогон: **64 passed**, без пропусков. Пять предупреждений SWIG/PyMuPDF сохраняются.
- Отдельный автономный прогон `test_robustness.py -m "not source_documents"`: **18 passed, 7 deselected**. Source-fixtures в этом прогоне не вызываются.
- Новый последовательный запуск `pnpm --dir frontend build`: TypeScript и Vite production-сборка прошли. `git diff --check` — без ошибок. Демо-сервер не запускался; новые замеры скорости анализа не выполнялись.
- 25 новых проверок в `backend/tests/test_robustness.py`: 18 синтетических без исходников организатора и 7 на изменённых в памяти копиях реальных DOCX. Исходные файлы не изменялись.
- Воспроизведены и устранены четыре исходных сбоя: выдуманный Главный аудитор для неизвестного §5.1, нераспознанные обязанности при перенумерации синтетического документа, потеря переносов при изменении двух пунктов реальной пары и при перенумерации всей иерархии.
- В варианте §5.4.4 → §5.4.14 / §5.3.3 → §5.3.14 и при независимой перенумерации обеих редакций сохраняются типы выводов и физические позиции доказательств, включая **два MOVED**. Ссылки показывают новые номера. Проверяется наличие доказательств обеих версий у каждого изменения подразделения.
- Отрицание, перефразирование, конкурирующий владелец, неверная принадлежность родительскому заголовку, сужение контекста, смена права на запрет и изменение числового условия не дают автоматического подтверждения. Сохранённая старая роль блокирует утверждение о её замене.
- Набор из 22 source-truth случаев и все прежние тесты проходят без изменения эталонных ожиданий. `human_signoff=null`: независимой экспертной приёмки по-прежнему нет.

Синтетические кейсы разработаны отдельно от демонстрационных текстов, но той же разработкой; они проверяют конкретные свойства, а не доказывают универсальную точность. Названия разделов и шаблоны специальных правил по-прежнему ограничивают поддерживаемое семейство документов. Семантическая эквивалентность перефразирования автоматически не подтверждается. На этом историческом этапе новые дубли без старой функции, комплекты документов и агентное расследование ещё не были реализованы; они добавлены в последующем этапе выше.

## Проверки до перезапуска (исторические результаты)

- `python -m pytest -q`: **39 passed**, без пропущенных source-тестов. Пять предупреждений относятся к типам SWIG в установленной версии PyMuPDF.
- `pnpm --dir frontend build`: TypeScript и Vite production-сборка проходят.
- `git diff --check`: ошибок пробелов нет.
- `scripts/benchmark.py`: первый API-анализ реальных DOCX примерно **1,51 с**, повтор из кэша **0,13 с**; canonical JSON идентичен. Это измерение этой машины, не SLA.
- В браузере проверены загрузка обеих реальных DOCX через file chooser, запуск анализа, обзор, подразделения, открытие точных цитат и контраргументов, список кандидатов, журнал поиска по новой версии и блокировка сохранения пустого решения.
- Сохранение, повторное чтение решений после открытия новой SQLite-сессии и неизменность export после решения покрыты API-тестом.
- Реальные вызовы OpenAI не выполнялись. Проверены feature flag, структурированный запрос, допустимость ID, недопустимость выдуманных фактов, кэш успешных и отвергнутых ответов, fallback на транспортную ошибку.

При установке зависимостей возникала нехватка диска. Удалены только временные кэши и тестовые артефакты этой задачи; frontend установлен через pnpm. Дублирование полного record в кэше запроса устранено, соединения SQLite закрываются явно. Для ошибки записи добавлены HTTP 507 и понятное сообщение пользователю, защищённые тестом.

Truth set содержит 22 случая с цитатами и хэшами. Независимая экспертная приёмка не выполнялась; `human_signoff=null`. Ограничения структурного парсера и форматов задокументированы в README, интерфейсе и audit record.
